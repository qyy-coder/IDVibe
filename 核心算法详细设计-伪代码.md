# 核心算法详细设计：伪代码与数学描述

> 配套文档：[技术创新方案-详细设计.md](./技术创新方案-详细设计.md)

---

## 目录

- [A. 合规检测引擎核心算法](#a-合规检测引擎核心算法)
- [B. 级联抠图边缘优化算法](#b-级联抠图边缘优化算法)
- [C. 光照估计与背景融合算法](#c-光照估计与背景融合算法)
- [D. 肤色自适应色彩管线](#d-肤色自适应色彩管线)
- [E. 拍摄实时引导状态机](#e-拍摄实时引导状态机)

---

## A. 合规检测引擎核心算法

### A.1 头部占比检测

```
Algorithm: Head_Ratio_Check
────────────────────────────────────────────
Input:  image (H×W×3), face_landmarks (468 points from MediaPipe), image_height H
Output: ratio (float), verdict (PASS|FAIL), correction_hint (string)

1  // 提取关键点索引 (MediaPipe Face Mesh)
2  forehead_idx  ← 10      // 额头中心
3  chin_idx      ← 152     // 下颚最低点
4  left_eye_idx  ← 33      // 左眼内角
5  right_eye_idx ← 263     // 右眼内角
6
7  // 获取像素坐标
8  forehead_y ← face_landmarks[forehead_idx].y * H
9  chin_y     ← face_landmarks[chin_idx].y * H
10
11 // 计算头部实际高度（含发际线补偿）
12 // 发际线在额头关键点上方约 8% 的头部高度
13 head_raw_height ← chin_y - forehead_y
14 hairline_offset ← head_raw_height * 0.08
15 head_top_y ← forehead_y - hairline_offset
16 head_height ← chin_y - head_top_y
17
18 // 计算占比
19 head_ratio ← head_height / H
20
21 // 合规判定 (ISO/IEC 19794-5: 70%-80%)
22 if head_ratio ≥ 0.70 and head_ratio ≤ 0.80 then
23     return (head_ratio, PASS, null)
24 else if head_ratio < 0.70 then
25     hint ← "头部占比偏小({:.1%})，请靠近镜头或裁剪画面".format(head_ratio)
26     return (head_ratio, FAIL, hint)
27 else
28     hint ← "头部占比偏大({:.1%})，请远离镜头".format(head_ratio)
29     return (head_ratio, FAIL, hint)
```

### A.2 三维姿态估计

```
Algorithm: Pose_Estimation
────────────────────────────────────────────
Input:  face_landmarks (468 points), image_size (W, H)
Output: yaw, pitch, roll (degrees), verdict

1  // 定义3D通用人脸模型关键点 (世界坐标系, 归一化)
2  model_3d ← {
3      nose_tip:       (0.0,  0.0,  0.0),
4      chin:           (0.0, -0.35, 0.05),
5      left_eye_inner: (-0.09, 0.05, 0.02),
6      right_eye_inner: (0.09, 0.05, 0.02),
7      left_mouth:     (-0.09, -0.18, 0.01),
8      right_mouth:    (0.09, -0.18, 0.01)
9  }
10
11 // 提取对应的2D观测点
12 image_2d ← ExtractCorrespondingLandmarks(face_landmarks, image_size)
13
14 // 构造相机内参矩阵 (近似)
15 focal_length ← max(W, H)
16 camera_matrix ← [
17     [focal_length, 0,           W/2],
18     [0,            focal_length, H/2],
19     [0,            0,           1  ]
20 ]
21
22 // EPnP解算
23 dist_coeffs ← zeros(4)  // 假设无畸变
24 success, rotation_vec, translation_vec ← solvePnP(
25     model_3d, image_2d, camera_matrix, dist_coeffs,
26     flags = SOLVEPNP_EPNP
27 )
28
29 // 旋转向量 → 旋转矩阵 → 欧拉角
30 rotation_matrix ← Rodrigues(rotation_vec)
31 yaw, pitch, roll ← DecomposeToEuler(rotation_matrix)
32
33 // 判定
34 threshold ← 5.0  // 度
35 if |yaw| < threshold and |pitch| < threshold and |roll| < threshold then
36     return (yaw, pitch, roll, PASS)
37 else
38     guidance ← GeneratePoseGuidance(yaw, pitch, roll)  // 见A.3
39     return (yaw, pitch, roll, FAIL, guidance)
```

### A.3 姿态反向引导生成

```
Algorithm: GeneratePoseGuidance
────────────────────────────────────────────
Input:  yaw, pitch, roll (degrees)
Output: guidance_text (string)

1  guidance ← []
2
3  // 偏航角: 正值 = 脸朝右, 负值 = 脸朝左
4  if yaw > 5.0 then
5      guidance.append("请将头部向左转约{:.0f}度".format(yaw - 5.0))
6  else if yaw < -5.0 then
7      guidance.append("请将头部向右转约{:.0f}度".format(-yaw - 5.0))
8
9  // 俯仰角: 正值 = 低头, 负值 = 抬头
10 if pitch > 5.0 then
11     guidance.append("请稍微抬起下巴")
12 else if pitch < -5.0 then
13     guidance.append("请稍微收低下巴")
14
15 // 翻滚角: 正值 = 头右倾, 负值 = 头左倾
16 if roll > 5.0 then
17     guidance.append("请将头部向左修正")
18 else if roll < -5.0 then
19     guidance.append("请将头部向右修正")
20
21 return "；".join(guidance)
```

### A.4 背景均匀度检测

```
Algorithm: Background_Uniformity_Check
────────────────────────────────────────────
Input:  image (H×W×3), alpha_matte (H×W)
Output: uniformity_score (0-100), has_shadow (bool), verdict

1  // 提取纯背景像素 (alpha < 0.05)
2  bg_mask ← alpha_matte < 0.05
3  bg_pixels ← image[bg_mask]
4
5  if len(bg_pixels) < 1000 then
6      return (0, false, FAIL)  // 无足够背景像素
7
8  // 转换到Lab色彩空间
9  bg_lab ← RGB2Lab(bg_pixels)
10 L_channel ← bg_lab[:, 0]
11 a_channel ← bg_lab[:, 1]
12 b_channel ← bg_lab[:, 2]
13
14 // 计算 a*, b* 通道的联合方差
15 var_a ← Variance(a_channel)
16 var_b ← Variance(b_channel)
17 combined_var ← var_a + var_b
18
19 // 映射到0-100评分 (方差越小越均匀)
20 var_threshold ← 25.0  // 经验阈值
21 uniformity_score ← max(0, 100 - (combined_var / var_threshold * 100))
22
23 // 阴影检测: 网格分块分析
24 grid_size ← 8
25 block_h ← H // grid_size
26 block_w ← W // grid_size
27 shadow_blocks ← 0
28
29 for i ← 0 to grid_size-1 do
30     for j ← 0 to grid_size-1 do
31         block ← image[i*block_h:(i+1)*block_h, j*block_w:(j+1)*block_w]
32         block_bg_mask ← bg_mask[i*block_h:(i+1)*block_h, j*block_w:(j+1)*block_w]
33         if BlockHasBackground(block_bg_mask) then
34             block_bg_lab ← RGB2Lab(block[block_bg_mask])
35             block_L_mean ← Mean(block_bg_lab[:, 0])
36
37             // 全局背景L均值
38             global_L_mean ← Mean(L_channel)
39
40             // 某块明显偏暗 → 可能有阴影
41             if block_L_mean < global_L_mean * 0.85 then
42                 shadow_blocks ← shadow_blocks + 1
43
44 has_shadow ← shadow_blocks > 0
45
46 // 综合判定
47 if uniformity_score ≥ 85 and not has_shadow then
48     return (uniformity_score, has_shadow, PASS)
49 else
50     return (uniformity_score, has_shadow, FAIL)
```

---

## B. 级联抠图边缘优化算法

### B.1 引导滤波边缘精细化

```
Algorithm: GuidedFilter_AlphaRefine
────────────────────────────────────────────
Input:  image (H×W×3), coarse_alpha (H×W), radius r=8, eps ε=1e-6
Output: refined_alpha (H×W)

// 数学原理:
//   引导滤波假设输出 q 是引导图 I 在局部窗口 ω_k 内的线性变换:
//     q_i = a_k · I_i + b_k,  ∀i ∈ ω_k
//   cost function: E(a_k, b_k) = Σ_{i∈ω_k} ((a_k·I_i + b_k - p_i)^2 + ε·a_k^2)
//   最优解:
//     a_k = cov(I,p) / (var(I) + ε)
//     b_k = mean(p) - a_k · mean(I)
//   最终输出取各窗口均值:
//     q_i = mean(a)_i · I_i + mean(b)_i

1  // Step 1: 准备引导图（灰度）
2  if image has 3 channels then
3      guide ← RGB2Gray(image) / 255.0
4  else
5      guide ← image / 255.0
6
7  src ← coarse_alpha  // 输入alpha作为待滤波信号p
8
9  // Step 2: 盒式滤波计算局部统计量 (cv2.boxFilter 实现 O(N) 复杂度)
10 mean_I  ← BoxFilter(guide,  r)       // mean(I)
11 mean_p  ← BoxFilter(src,    r)        // mean(p)
12 mean_Ip ← BoxFilter(guide * src, r)   // mean(I·p)
13 corr_I  ← BoxFilter(guide * guide, r) // mean(I²)
14
15 // Step 3: 计算局部方差和协方差
16 var_I   ← corr_I - mean_I * mean_I    // var(I) = E[I²] - E[I]²
17 cov_Ip  ← mean_Ip - mean_I * mean_p   // cov(I,p) = E[I·p] - E[I]·E[p]
18
19 // Step 4: 计算线性系数
20 a ← cov_Ip / (var_I + ε)
21 b ← mean_p - a * mean_I
22
23 // Step 5: 取窗口均值
24 mean_a ← BoxFilter(a, r)
25 mean_b ← BoxFilter(b, r)
26
27 // Step 6: 重构输出
28 refined_alpha ← mean_a * guide + mean_b
29
30 return Clip(refined_alpha, 0, 1)
```

### B.2 边缘置信度评估与自适应回退

```
Algorithm: AdaptiveMatting_WithFallback
────────────────────────────────────────────
Input:  image, coarse_alpha, refined_alpha
Output: final_alpha, quality_report

1  // 定义不确定区域 (alpha过渡区)
2  uncertain_mask ← (coarse_alpha > 0.05) AND (coarse_alpha < 0.95)
3  uncertain_pixel_count ← Count(uncertain_mask)
4  total_pixel_count ← H × W
5  uncertain_ratio ← uncertain_pixel_count / total_pixel_count
6
7  // 在不确定区域内计算边缘梯度
8  grad_x ← SobelX(coarse_alpha)
9  grad_y ← SobelY(coarse_alpha)
10 gradient_magnitude ← Sqrt(grad_x² + grad_y²)
11
12 // 仅统计不确定区域内的梯度
13 uncertain_grad ← gradient_magnitude[uncertain_mask]
14 mean_edge_strength ← Mean(uncertain_grad)
15 max_edge_strength  ← Max(uncertain_grad)
16
17 // 置信度评分 (0-100)
18 // 过渡区域越小、边缘越清晰 → 置信度越高
19 area_penalty ← uncertain_ratio * 100
20 edge_bonus  ← min(mean_edge_strength * 200, 30)  // 边界清晰加分
21 confidence  ← max(0, min(100, 100 - area_penalty + edge_bonus))
22
23 // 决策逻辑
24 if confidence ≥ 70 then
25     // 高质量: 使用引导滤波结果
26     final_alpha ← refined_alpha
27     quality_report ← { quality: "high", method: "GuidedFilter" }
28
29 else if confidence ≥ 40 then
30     // 中等质量: 混合使用 (硬边缘用细化结果，软边缘用原始结果)
31     blend_weight ← (confidence - 40) / 30  // 归一化到 0-1
32     final_alpha ← refined_alpha * blend_weight + coarse_alpha * (1 - blend_weight)
33     quality_report ← { quality: "medium", method: "Blend" }
34
35 else
36     // 低质量: 触发回退 - 对不确定区域使用RMBG-2.0重算
37     fallback_alpha ← RMBG2_Inference(image, focus_mask=uncertain_mask)
38     // 仅替换不确定区域
39     final_alpha ← Where(uncertain_mask, fallback_alpha, refined_alpha)
40     quality_report ← { quality: "fallback", method: "RMBG-2.0_retouch" }
41
42 return final_alpha, quality_report
```

---

## C. 光照估计与背景融合算法

### C.1 球谐光照估计

```
Algorithm: SH_Lighting_Estimation
────────────────────────────────────────────
Input:  face_region (128×128×3)
Output: SH_coefficients (27-dim vector: 9 coefficients × 3 RGB channels)

// 球谐函数 (Spherical Harmonics) 光照模型:
// 使用前3阶 (0,1,2) 共9个基函数近似环境光照
// E(n) ≈ Σ_{l=0}^{2} Σ_{m=-l}^{l} c_{lm} · Y_{lm}(n)
//
// 其中 Y_{lm} 为球谐基函数:
//   Y_{00}(θ,φ) = 1 / sqrt(4π)
//   Y_{1-1}(θ,φ) = sqrt(3/(4π)) · sin(θ) · sin(φ)
//   Y_{10}(θ,φ)  = sqrt(3/(4π)) · cos(θ)
//   Y_{11}(θ,φ)  = sqrt(3/(4π)) · sin(θ) · cos(φ)
//   ... 高阶类似

1  // 使用预训练的轻量光照估计网络
2  lightnet ← LoadONNX("lightnet_int8.onnx")  // ~500KB
3
4  // 预处理
5  face_normalized ← face_region / 255.0
6  face_normalized ← (face_normalized - mean) / std
7
8  // 推理
9  SH_coefficients ← lightnet.Inference(face_normalized)
10 // 输出: (27,) — 9个SH系数 × 3个RGB通道
11
12 return SH_coefficients
```

### C.2 球谐驱动软阴影生成

```
Algorithm: Generate_SoftShadow
────────────────────────────────────────────
Input:  SH_coefficients, alpha_matte, image_size (W, H), background_color
Output: shadow_overlay (H×W)

1  // 从SH系数重建环境光方向
2  // 主光源方向由1阶系数 (Y_{1-1}, Y_{10}, Y_{11}) 决定
3  L_x ← SH_coefficients[1:2].mean()   // 与Y_{1-1}相关 → x方向
4  L_y ← SH_coefficients[2:3].mean()   // 与Y_{10}相关  → y方向
5  L_z ← SH_coefficients[3:4].mean()   // 与Y_{11}相关  → z方向
6
7  dominant_light_dir ← Normalize([L_x, L_y, L_z])
8
9  // 如果主光源来自上方 (典型的室内顶部灯光)
10 if dominant_light_dir.y < 0.2 then
11     // 弱或无定向光源 → 不生成阴影 (均匀光照)
12     return zeros(H, W)
13
14 // 对人体轮廓进行形态学膨胀（生成阴影区域）
15 person_silhouette ← alpha_matte > 0.5
16 kernel_size ← int(20 * dominant_light_dir.y)  // 光源越高 → 阴影越短
17 shadow_kernel ← EllipseKernel(kernel_size, kernel_size * 0.5)
18
19 // 按光源方向偏移 (光源在左上 → 阴影在右下)
20 offset_x ← int(15 * (1 - dominant_light_dir.x))
21 offset_y ← int(15 * dominant_light_dir.y)
22
23 // 膨胀 + 平移
24 dilated ← Dilate(person_silhouette, shadow_kernel)
25 shadow_mask ← TranslateAndFade(dilated, offset_x, offset_y, alpha_matte)
26
27 // 软阴影: 高斯模糊
28 shadow_soft ← GaussianBlur(shadow_mask, sigma=8)
29
30 // 阴影透明度 (5-12%, 非常微妙)
31 shadow_intensity ← 0.08 + 0.04 * (1 - dominant_light_dir.y)
32 shadow_overlay ← shadow_soft * shadow_intensity
33
34 return shadow_overlay
```

### C.3 边缘色彩去溢出

```
Algorithm: Color_Decontamination
────────────────────────────────────────────
Input:  image (H×W×3), alpha_matte (H×W), original_bg_color (RGB)
Output: decontaminated_image (H×W×3)

1  // Step 1: 定位过渡区域
2  transition_mask ← (alpha_matte > 0.1) AND (alpha_matte < 0.85)
3
4  // Step 2: 定位"纯净前景"区域 (不受溢出影响的参考区)
5  pure_fg_mask ← alpha_matte > 0.95
6  pure_fg_pixels ← image[pure_fg_mask]
7
8  if len(pure_fg_pixels) < 500 then
9      return image  // 纯净前景不足，不做处理
10
11 // Step 3: 对每个过渡区像素检测溢出
12 decontaminated ← Copy(image)
13
14 for each pixel p where transition_mask[p] is True do
15     pixel_rgb ← image[p]
16
17     // 计算与原始背景色的马氏距离
18     mahal_dist ← MahalanobisDistance(pixel_rgb, original_bg_color, bg_covariance)
19
20     if mahal_dist < 3.0 then
21         // 该像素可能含有溢出的背景色
22         // 在纯净前景中找最近邻颜色
23         nearest ← NearestNeighbor(pixel_rgb, pure_fg_pixels, k=3)
24
25         // 颜色修正: 在Lab空间进行混合
26         pixel_lab ← RGB2Lab(pixel_rgb)
27         nearest_lab ← RGB2Lab(Mean(nearest))
28
29         // 仅修正 a*, b* (色度)，保留 L (亮度)
30         blend_factor ← 1.0 - alpha_matte[p]  // alpha越低，修正越强
31         pixel_lab[1] ← (1-blend_factor) * nearest_lab[1] + blend_factor * pixel_lab[1]
32         pixel_lab[2] ← (1-blend_factor) * nearest_lab[2] + blend_factor * pixel_lab[2]
33
34         decontaminated[p] ← Lab2RGB(pixel_lab)
35
36 return decontaminated
```

---

## D. 肤色自适应色彩管线

### D.1 Fitzpatrick 肤色分类

```
Algorithm: SkinTone_Classifier
────────────────────────────────────────────
Input:  face_region (H×W×3), face_landmarks
Output: fitzpatrick_type (I-VI)

1  // Step 1: 精确提取肤色区域
2  // 使用Indices方法提取面部核心区域（排除眼睛、眉毛、嘴唇）
3  face_oval_indices ← [10, 338, 297, 332, 284, 251, 389, 356,
4                        454, 323, 361, 288, 397, 365, 379, 378,
5                        400, 377, 152, 148, 176, 149, 150, 136,
6                        172, 58, 132, 93, 234, 127, 162, 21, 54]
7
8  face_oval_points ← face_landmarks[face_oval_indices]
9  face_mask ← FillConvexHull(face_oval_points)
10
11 // Step 2: 排除高光和阴影区域
12 face_lab ← RGB2Lab(face_region)
13 L_channel ← face_lab[:,:,0]
14
15 // 高光: L > μ_L + 1.5σ
16 highlight_mask ← L_channel > Mean(L_channel[face_mask]) + 1.5 * Std(L_channel[face_mask])
17 // 阴影: L < μ_L - 1.2σ
18 shadow_mask ← L_channel < Mean(L_channel[face_mask]) - 1.2 * Std(L_channel[face_mask])
19
20 skin_mask ← face_mask AND NOT highlight_mask AND NOT shadow_mask
21
22 // Step 3: 在YCbCr空间计算肤色统计量
23 face_ycbcr ← RGB2YCbCr(face_region)
24 cb_values ← face_ycbcr[skin_mask, 1]
25 cr_values ← face_ycbcr[skin_mask, 2]
26
27 μ_cb ← Mean(cb_values)
28 μ_cr ← Mean(cr_values)
29
30 // Step 4: Fitzpatrick分类 (基于经验阈值)
31 // 肤色越深 → Cb值越高, Cr值越高
32 if μ_cb ≥ 120 and μ_cr ≥ 150 then
33     fitzpatrick_type ← V_or_VI  // 深肤色
34 else if μ_cb ≥ 115 and μ_cr ≥ 145 then
35     fitzpatrick_type ← IV       // 橄榄肤色
36 else if μ_cb ≥ 110 and μ_cr ≥ 140 then
37     fitzpatrick_type ← III      // 中等肤色
38 else
39     fitzpatrick_type ← I_or_II  // 白皙肤色
40
41 return fitzpatrick_type
```

### D.2 自适应美颜参数

```
Algorithm: Adaptive_Beautification_Params
────────────────────────────────────────────
Input:  fitzpatrick_type
Output: beautification_params (dict)

1  params ← {}
2
3  switch fitzpatrick_type do
4      case I_or_II:   // 白皙肤质
5          params.smoothing_sigma  ← 1.2
6          params.contrast_boost   ← 1.05
7          params.brightness_adj   ← +3
8          params.saturation_adj   ← 1.00
9          params.sharpening       ← 1.10
10
11     case III:       // 中等肤质
12         params.smoothing_sigma  ← 1.5
13         params.contrast_boost   ← 1.10
14         params.brightness_adj   ← +5
15         params.saturation_adj   ← 1.02
16         params.sharpening       ← 1.15
17
18     case IV:        // 橄榄肤质
19         params.smoothing_sigma  ← 1.3
20         params.contrast_boost   ← 1.08
21         params.brightness_adj   ← +5
22         params.saturation_adj   ← 1.03
23         params.sharpening       ← 1.12
24
25     case V_or_VI:   // 深色肤质
26         params.smoothing_sigma  ← 1.0   // 轻度处理，避免灰白感
27         params.contrast_boost   ← 1.00
28         params.brightness_adj   ← +2
29         params.saturation_adj   ← 1.00  // 保持原始肤色饱和度
30         params.sharpening       ← 1.15
31
32 return params
```

### D.3 肤色感知白平衡

```
Algorithm: SkinAware_WhiteBalance
────────────────────────────────────────────
Input:  image, skin_mask, target_illuminant=[0.33, 0.33] (D65白点)
Output: balanced_image

1  // Step 1: 分离肤色区域和非肤色区域
2  skin_pixels      ← image[skin_mask]
3  non_skin_pixels  ← image[NOT skin_mask]
4
5  // Step 2: 使用Gray World假设估计非肤色区域的全局光照
6  // Gray World: 假设场景的平均反射率为灰色 → 各通道均值相等
7  avg_non_skin_R ← Mean(non_skin_pixels[:,0])
8  avg_non_skin_G ← Mean(non_skin_pixels[:,1])
9  avg_non_skin_B ← Mean(non_skin_pixels[:,2])
10
11 overall_avg ← (avg_non_skin_R + avg_non_skin_G + avg_non_skin_B) / 3
12
13 // 仅对非肤色区域计算校正增益
14 gain_R ← overall_avg / avg_non_skin_R
15 gain_G ← overall_avg / avg_non_skin_G
16 gain_B ← overall_avg / avg_non_skin_B
17
18 // Step 3: 记录肤色原始统计量（用于约束）
19 avg_skin_Cb_before, avg_skin_Cr_before ← YCbCr_Stats(image, skin_mask)
20
21 // Step 4: 应用白平衡（全图）
22 balanced_image ← Copy(image)
23 balanced_image[:,:,0] ← image[:,:,0] * gain_R
24 balanced_image[:,:,1] ← image[:,:,1] * gain_G
25 balanced_image[:,:,2] ← image[:,:,2] * gain_B
26
27 // Step 5: 肤色约束 - 确保校正后肤色仍在自然范围
28 avg_skin_Cb_after, avg_skin_Cr_after ← YCbCr_Stats(balanced_image, skin_mask)
29
30 ΔCb ← avg_skin_Cb_after - avg_skin_Cb_before
31 ΔCr ← avg_skin_Cr_after - avg_skin_Cr_before
32
33 // 若肤色偏移 ≥ 5个单位，回退肤色区域到接近原始值
34 if |ΔCb| ≥ 5 or |ΔCr| ≥ 5 then
35     // 对肤色区域进行插值融合
36     correction_weight ← min(3.0 / max(|ΔCb|, |ΔCr|), 1.0)
37     balanced_image[skin_mask] ← (
38         image[skin_mask] * (1 - correction_weight) +
39         balanced_image[skin_mask] * correction_weight
40     )
41
42 return balanced_image
```

---

## E. 拍摄实时引导状态机

### E.1 全流程状态机

```
Algorithm: CaptureGuidance_StateMachine
────────────────────────────────────────────
States: POSE → EXPRESSION → LIGHTING → READY

1  guidance_state ← POSE
2
3  loop every frame (~30fps) do
4      frame ← CaptureCameraFrame()
5      face_detected, landmarks ← DetectFace(frame)
6
7      if not face_detected then
8          DisplayOverlay("未检测到人脸，请将面部置于画面中央")
9          continue
10
11     switch guidance_state do
12         case POSE:
13             yaw, pitch, roll ← EstimatePose(landmarks)
14             pose_ok ← |yaw|<5° AND |pitch|<5° AND |roll|<5°
15
16             if pose_ok then
17                 guidance_state ← EXPRESSION
18                 PlaySound("pose_ok_chime")
19             else
20                 guidance ← GeneratePoseGuidance(yaw, pitch, roll)
21                 DisplayOverlay(guidance + DrawPoseGuideLines(landmarks))
22
23         case EXPRESSION:
24             eyes_open ← CheckEyesOpen(landmarks)
25             mouth_closed ← CheckMouthClosed(landmarks)
26             has_smile ← DetectSmile(frame, landmarks)
27
28             if eyes_open AND mouth_closed AND NOT has_smile then
29                 guidance_state ← LIGHTING
30                 PlaySound("expression_ok_chime")
31             else
32                 if not eyes_open then
33                     DisplayOverlay("请睁开双眼")
34                 else if not mouth_closed then
35                     DisplayOverlay("请闭合嘴唇，保持中性表情")
36                 else if has_smile then
37                     DisplayOverlay("请保持严肃，不要微笑")
38
39         case LIGHTING:
40             face_region ← CropFace(frame, landmarks)
41             uniformity, has_shadow, brightness ← AssessLighting(face_region)
42
43             if uniformity > 0.8 AND NOT has_shadow AND 100 < brightness < 200 then
44                 guidance_state ← READY
45                 PlaySound("ready_chime")
46             else
47                 if uniformity <= 0.8 then
48                     DisplayOverlay("面部光照不均匀，请调整光源位置")
49                 else if has_shadow then
50                     DisplayOverlay("检测到面部阴影，请避免侧光")
51                 else if brightness < 100 then
52                     DisplayOverlay("环境过暗，请增加光线")
53                 else if brightness > 200 then
54                     DisplayOverlay("环境过亮，请避免直射强光")
55
56         case READY:
57             DisplayOverlay("✅ 就绪 — 保持姿势，3秒后自动拍摄")
58             StartCountdown(3)
59             if CountdownExpired() then
60                 CaptureAndProcess(frame)
61                 return SUCCESS
62
63             // 若就绪状态被打破，回退
64             if not AllChecksPass(frame) then
65                 guidance_state ← POSE
66                 DisplayOverlay("姿态已变化，请重新调整")
```

---

## 附录：项目文件结构建议

```
IDPhotoSystem/
├── inference/
│   ├── detection/
│   │   ├── scrfd_face_detector.py        # 人脸检测
│   │   └── mediapipe_landmark.py          # 人脸关键点
│   ├── matting/
│   │   ├── modnet_matting.py              # 粗抠图 (基于开源MODNet)
│   │   ├── guided_filter_refine.py        # ★ 自研: 边缘精细化
│   │   └── adaptive_fallback.py           # ★ 自研: 自适应回退
│   ├── beautification/
│   │   ├── lq_beautynet.py                # ★ 自研: 轻量美颜网络
│   │   └── skin_tone_pipeline.py          # ★ 自研: 肤色自适应
│   ├── compliance/
│   │   ├── geometric_checks.py            # ★ 自研: 几何检测
│   │   ├── pose_estimation.py             # ★ 自研: 姿态估计
│   │   ├── lighting_checks.py             # ★ 自研: 光照检测
│   │   └── quality_checks.py              # ★ 自研: 质量检测
│   └── lighting/
│       ├── sh_estimator.py                # ★ 自研: 球谐光照估计
│       └── shadow_synthesis.py            # ★ 自研: 软阴影合成
├── pipeline/
│   ├── idphoto_pipeline.py                # 主流水线
│   └── capture_guidance.py                # ★ 自研: 拍摄引导
├── models/                                # ONNX模型文件
│   ├── modnet.onnx                        # 开源
│   ├── scrfd_2.5g.onnx                    # 开源
│   ├── beautynet_int8.onnx                # ★ 自研
│   ├── lightnet_int8.onnx                 # ★ 自研
│   └── compliance_classifier.onnx         # ★ 自研
├── quantization/                          # ★ 自研: 量化工具
│   ├── calibrate.py
│   └── quantize_pipeline.py
└── tests/
    ├── test_compliance.py
    ├── test_matting.py
    └── benchmark.py
```

> 带 ★ 的为团队自研模块（共11个核心模块）

---

> **文档状态**: 待团队评审 | **下次更新**: 补充实验数据后
