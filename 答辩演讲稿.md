# AI一照成证 — 项目答辩演讲稿

> 总时长约5分钟，对应10页PPT

---

## Slide 1: 封面 (15秒)

各位老师好，我今天汇报的项目是"AI一照成证"—— 一套智能证件照生成与优化系统。

一句话概括：用户上传任意照片，系统在3秒内自动生成符合标准、可直接使用的证件照。整个过程完全在本地运行，不会上传任何数据到云端。

---

## Slide 2: 为什么做这个项目 (45秒)

我们分析了现有证件照产品的三个核心痛点。

**第一，隐私问题。** 市面上主流的证件照App，包括支付宝、最美证件照，都需要将用户的原始照片上传到云端服务器处理。在数据泄露频发的今天，这是一个很大的隐患。

**第二，质量不可控。** 很多用户自己拍的照片提交后被退回——因为头部占比不合规、背景色不达标、或者光线不均匀。没有一个事前检测的机制。

**第三，操作繁琐。** 如果要准备白底、蓝底、红底三张照片，需要反复操作三次。

我们的方案很简单：全本地AI处理，加上实时智能检测，一次点击生成三种底色。

---

## Slide 3: 我们做了什么 (60秒)

项目分两个阶段完成。

**P0阶段**搭建了完整的流水线：从人脸检测、人像抠图、到背景替换和智能裁剪，形成闭环。支持10种规格、3种背景色。

**P1阶段**我们做了两个自研创新模块。

第一个是"级联边缘优化"。HivisionIDPhotos原生的MODNet抠图，在发丝、半透明区域会有锯齿。我们在它后面加了一个引导滤波器——Guided Filter——专门优化边缘。O(N)复杂度的盒式滤波实现，发丝通过率从75%提升到92%以上。

第二个是"智能合规检测"。8项检测覆盖头部大小、光照、清晰度等。关键是阈值是自适应的——远距离自拍和近距离半身照用不同的标准，不会动不动就报FAIL。

---

## Slide 4: 系统架构 (30秒)

这是我们的技术架构。

前端用原生HTML/CSS/JS，零框架依赖。后端FastAPI提供REST接口。核心推理基于HivisionIDPhotos的MTCNN加MODNet组合。我们自研的引导滤波和智能检测作为插件嵌入。

模型总大小只有25MB，CPU就能跑，不需要GPU。

---

## Slide 5: 自研创新点 (40秒)

两个创新点再详细说一下。

级联抠图是三阶段的：粗抠图→引导滤波细化→置信度评分加回退。数学原理是局部线性模型的最优解，我们用OpenCV的boxFilter实现O(N)复杂度。

智能检测的核心是自适应。传统的固定阈值方案对普通用户不友好，我们根据人脸在画面中的占比自动判断是远距离还是近距离拍摄，相应调整标准。只对真正严重的问题报warn，轻微问题给建议但不阻止使用。

第三个创新是隐私。所有处理100%本地完成，没有任何网络请求。这是和市面上所有竞品最根本的区别。

---

## Slide 6-7: 关键代码 (30秒)

这里展示了两段核心代码。

左边是引导滤波的实现，10行Python，用盒式滤波实现O(N)的边缘感知平滑。这是整个系统里数学最密集的部分。

右边是智能检测引擎的自适应逻辑，根据人脸大小判断拍摄距离，自动调整检测标准。

---

## Slide 8: 成果与验证 (30秒)

所有验收指标全部达成。

处理速度3秒以内，模型25MB，44项自动化测试100%通过。代码托管在GitHub上，有完整的README和一键启动脚本。

我们的目标是在性能和隐私之间找到最佳平衡，目前的数据表明这个目标已经达到。

---

## Slide 9: Vibe Coding 开发历程 (25秒)

这个项目还有一个特别之处——全程由AI辅助编码完成。

四个阶段：需求澄清用3轮对话确定P0到P1的路线；方案生成阶段对比了自研和基于开源的方案；44次提交完成实现迭代；调试过程中解决了合规误报和状态管理等关键问题。

AI在代码生成和调试方面非常高效，但在UI创意和平台兼容性上还需要人工把控。

---

## Slide 10: 总结 (15秒)

总结一下：我们做了一套全本地、隐私零泄露的AI证件照系统。自研了级联抠图和智能检测两个创新模块。44项测试全部通过，可以直接使用。

感谢各位老师，欢迎提问。

---

## 可能的Q&A

**Q: 和支付宝证件照有什么区别？**
A: 支付宝需要上传照片到云端，我们的系统100%本地处理。另外我们有智能检测和发丝优化，支付宝没有。

**Q: 为什么要基于HivisionIDPhotos？**
A: 它的MTCNN和MODNet组合已经验证了基础抠图效果。我们选择在它的基础上做增量创新，而不是重新造轮子。这样竞赛评委能清楚看到哪些是开源、哪些是自研。

**Q: 手机端能用吗？**
A: 目前是Web版本，手机浏览器可以访问。原生小程序版本框架已完成，后续可以接入。
